node_modules/
.env
.env.local

# Build output (npm run build — see scripts/build.js) — generated from
# css/*.css and js/*.js, regenerated on deploy, never hand-edited.
/dist/

# Runtime state written by server.js's local (non-Supabase) fallback —
# accumulates real phone numbers from local testing, never something to ship.
data/*.json

# Personal reference material, not used by the running site (nothing in
# index.html/css/js points at these) — kept on disk, just not deployed.
/образец/
/оплата кд.jpg
/телеграм забота.jpg
