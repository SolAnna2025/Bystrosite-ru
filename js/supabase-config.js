/* ============================================================
   Быстросайт — Supabase project config (client-side).
   The anon/publishable key is meant to be public — it's what RLS exists
   to constrain, not a secret. Never put the service_role key here; that
   one stays server-side only (see server.js).
   ============================================================ */

window.BS_SUPABASE = {
  url: 'https://creturskbnqztuxzmjgf.supabase.co',
  anonKey: 'sb_publishable_AAi8TYLl9eaIiEQlh4VfiQ_IwMf_M22',
};
